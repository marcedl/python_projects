user_prompt = "Enter a to do:"

#Python methods: store in a list - append

todos = [] #A list outside the loop is creted to avoid overwritting

while True:
  todo = input(user_prompt)
  todos.append(todo)
  print(todos)
  



